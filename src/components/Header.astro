<header
    class={`
        flex justify-center items-center
        mx-auto
        py-3 px-4
        sticky top-0
        w-full xl:w-[1120px]
        z-10
        transition-all duration-300 ease-in-out
    `}
    id="header"
>
    <nav
        class={`
            border border-white/10
            rounded-full
            px-2 sm:px-4 py-2
            flex flex-row items-center
            gap-x-2 sm:gap-x-4 md:gap-x-6 lg:gap-x-10
            dark:bg-black/40
            backdrop-blur-2xl
            text-xs sm:text-sm
            shadow-lg shadow-black/5
            max-w-full
            overflow-hidden
        `}
    >
        <a class="hover:bg-white/10 rounded-full px-2 sm:px-3 py-1 transition whitespace-nowrap text-center min-w-0 flex-shrink" href="/#work-experience">
            <span class="hidden sm:inline">Work Experience</span>
            <span class="sm:hidden">Experience</span>
        </a>
        <a class="hover:bg-white/10 rounded-full px-2 sm:px-3 py-1 transition whitespace-nowrap text-center min-w-0 flex-shrink" href="/#projects">
            Projects
        </a>
        <a class="hover:bg-white/10 rounded-full px-2 sm:px-3 py-1 transition whitespace-nowrap text-center min-w-0 flex-shrink" href="/#about-me">
            <span class="hidden sm:inline">About me</span>
            <span class="sm:hidden">About</span>
        </a>
        <a class="hover:bg-white/10 rounded-full px-2 sm:px-3 py-1 transition whitespace-nowrap text-center min-w-0 flex-shrink" href="/#contact">
            Contact
        </a>
    </nav>
</header>

<script>
    const header = document.getElementById('header')
    
    window.addEventListener('scroll', () => {
        if (window.scrollY > 0) {
            header?.classList.add('scrolled')
        } else {
            header?.classList.remove('scrolled')
        }
    })
</script>

<style>
    .scrolled nav {
        @apply bg-black/50 border-white/20;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    }
    
    @media (max-width: 400px) {
        nav {
            gap: 0.25rem;
            padding: 0.5rem 0.75rem;
        }
        
        nav a {
            padding: 0.25rem 0.5rem;
            font-size: 0.65rem;
        }
    }
</style>